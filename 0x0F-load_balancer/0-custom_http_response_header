#!/usr/bin/env bash
# Comment text
name = $hostname
apt-get -y update
apt-get -y install nginx
ufw allow 'Nginx HTTP'
echo 'Hello World!' > /var/www/html/index.nginx-debian.html
echo "add_header X-Served-By $name" > /etc/nginx/nginx.conf
service nginx start
